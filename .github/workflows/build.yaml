# yamllint disable rule:line-length
---
name: Build add-on images

on:
  push:
    tags:
      - "bentopdf-*"
      - "bentopdf@*"
      - "manyfold_solo-*"
      - "manyfold_solo@*"
      - "stirling_pdf_fat-*"
      - "stirling_pdf_fat@*"
      - "stirling_pdf_full-*"
      - "stirling_pdf_full@*"
      - "stirling_pdf_ultra_lite-*"
      - "stirling_pdf_ultra_lite@*"
      - "nginx_webserver_proxy-*"
      - "nginx_webserver_proxy@*"
  workflow_dispatch:
    inputs:
      addon:
        description: 'Addon to build'
        required: false
        type: choice
        options:
          - all
          - bentopdf
          - manyfold_solo
          - stirling_pdf_fat
          - stirling_pdf_full
          - stirling_pdf_ultra_lite
          - nginx_webserver_proxy

jobs:
  build:
    name: ${{ matrix.addon }} / ${{ matrix.arch }}
    runs-on: ${{ matrix.runner }}
    permissions:
      contents: read
      packages: write
    if: |
      startsWith(github.ref_name, matrix.addon) ||
      (github.event_name == 'workflow_dispatch' && (inputs.addon == 'all' || inputs.addon == matrix.addon))
    strategy:
      fail-fast: false
      matrix:
        addon:
          - bentopdf
          - manyfold_solo
          - stirling_pdf_fat
          - stirling_pdf_full
          - stirling_pdf_ultra_lite
          - nginx_webserver_proxy
        arch:
          - amd64
          - aarch64
        include:
          - arch: amd64
            runner: ubuntu-24.04
          - arch: aarch64
            runner: ubuntu-24.04-arm
    steps:
      - name: Checkout
        uses: actions/checkout@v5

      - name: Read add-on metadata
        id: meta
        run: |
          VERSION=$(grep '^version:' ${{ matrix.addon }}/config.yaml | awk '{print $2}' | tr -d '"')
          IMAGE=$(grep '^image:' ${{ matrix.addon }}/config.yaml | awk '{print $2}' | tr -d '"' | sed 's/{arch}/${{ matrix.arch }}/g')
          echo "version=${VERSION}" >> "$GITHUB_OUTPUT"
          echo "image=${IMAGE}" >> "$GITHUB_OUTPUT"

      - name: Build and push bentopdf
        if: matrix.addon == 'bentopdf'
        uses: home-assistant/builder/actions/build-image@2026.03.2
        with:
          arch: ${{ matrix.arch }}
          context: bentopdf
          image: ${{ steps.meta.outputs.image }}
          image-tags: ${{ steps.meta.outputs.version }}
          version: ${{ steps.meta.outputs.version }}
          build-args: BUILD_FROM=ghcr.io/home-assistant/${{ matrix.arch }}-base:3.23
          cosign: "false"
          push: "true"
          container-registry-password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push manyfold_solo
        if: matrix.addon == 'manyfold_solo'
        uses: home-assistant/builder/actions/build-image@2026.03.2
        with:
          arch: ${{ matrix.arch }}
          context: manyfold_solo
          image: ${{ steps.meta.outputs.image }}
          image-tags: ${{ steps.meta.outputs.version }}
          version: ${{ steps.meta.outputs.version }}
          build-args: BUILD_FROM=ghcr.io/manyfold3d/manyfold-solo:latest
          cosign: "false"
          push: "true"
          container-registry-password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push stirling_pdf_fat
        if: matrix.addon == 'stirling_pdf_fat'
        uses: home-assistant/builder/actions/build-image@2026.03.2
        with:
          arch: ${{ matrix.arch }}
          context: stirling_pdf_fat
          image: ${{ steps.meta.outputs.image }}
          image-tags: ${{ steps.meta.outputs.version }}
          version: ${{ steps.meta.outputs.version }}
          build-args: BUILD_FROM=stirlingtools/stirling-pdf:latest-fat
          cosign: "false"
          push: "true"
          container-registry-password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push stirling_pdf_full
        if: matrix.addon == 'stirling_pdf_full'
        uses: home-assistant/builder/actions/build-image@2026.03.2
        with:
          arch: ${{ matrix.arch }}
          context: stirling_pdf_full
          image: ${{ steps.meta.outputs.image }}
          image-tags: ${{ steps.meta.outputs.version }}
          version: ${{ steps.meta.outputs.version }}
          build-args: BUILD_FROM=stirlingtools/stirling-pdf:latest
          cosign: "false"
          push: "true"
          container-registry-password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push stirling_pdf_ultra_lite
        if: matrix.addon == 'stirling_pdf_ultra_lite'
        uses: home-assistant/builder/actions/build-image@2026.03.2
        with:
          arch: ${{ matrix.arch }}
          context: stirling_pdf_ultra_lite
          image: ${{ steps.meta.outputs.image }}
          image-tags: ${{ steps.meta.outputs.version }}
          version: ${{ steps.meta.outputs.version }}
          build-args: BUILD_FROM=stirlingtools/stirling-pdf:latest-ultra-lite
          cosign: "false"
          push: "true"
          container-registry-password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push nginx_webserver_proxy
        if: matrix.addon == 'nginx_webserver_proxy'
        uses: home-assistant/builder/actions/build-image@2026.03.2
        with:
          arch: ${{ matrix.arch }}
          context: nginx_webserver_proxy
          image: ${{ steps.meta.outputs.image }}
          image-tags: ${{ steps.meta.outputs.version }}
          version: ${{ steps.meta.outputs.version }}
          build-args: BUILD_FROM=jc21/nginx-proxy-manager:latest
          cosign: "false"
          push: "true"
          container-registry-password: ${{ secrets.GITHUB_TOKEN }}
