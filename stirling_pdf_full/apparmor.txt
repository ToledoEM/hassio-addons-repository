#include <tunables/global>

profile hassio-addons/stirling_pdf_full flags=(attach_disconnected,mediate_deleted) {
  #include <abstractions/base>
  #include <abstractions/bash>
  #include <abstractions/nameservice>
  #include <abstractions/openssl>

  # Baseline profile for Stirling-PDF Full (JVM + LibreOffice + OCR tools) in HAOS.
  file,
  network,

  capability chown,
  capability dac_override,
  capability fowner,
  capability setgid,
  capability setuid,

  deny /proc/kcore rwklx,
  deny /proc/sysrq-trigger rwklx,
  deny /sys/firmware/** rwklx,
}
