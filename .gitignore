# macOS
.DS_Store

# Local scripts and logs
sync-addons.log
release-addons.log
addons.txt
release-addons-monorepo.sh

# Local NPM proxy configs (personal, not for the repo)
npm-*.json
npm-*.md

# Fetched at render time; CC BY-NC, not vendored into this MIT repo
.github/stats/xkcd.ttf
